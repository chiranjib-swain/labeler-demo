// A sample component
function Button({ label }) {
    return `<button>${label}</button> `;//testing this button and issue 870 again
  }
  module.exports = Button;

// This file is part of the labeler-demo project.